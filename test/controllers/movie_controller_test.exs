defmodule CordcutterApi.MovieControllerTest do
  use CordcutterApi.ConnCase

  # TODO: write unit tests for show and movie controllers

  # test "GET /", %{conn: conn} do
  #   conn = get conn, "/"
  #   assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  # end
end
